# Exercises

These files are reusable problem stubs. Each function contains its prompt and
raises `NotImplementedError` until implemented.

Run the exercise tests from the repository root:

```powershell
python -m unittest discover -s exercises/python
```

Untouched functions are skipped, so implement and test one function at a time.
The reviewed answers are kept separately in `solutions/`.
