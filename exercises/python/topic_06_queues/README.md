# Topic 06 - Queue Exercises

Read `lessons/06_queues.md` first.

Implement one function at a time in `queues.py`, then run:

```powershell
python -m unittest discover -s exercises/python
```

Unimplemented functions are skipped.

## Order

Queue operation warm-ups:

1. `process_queue_operations`
2. `serve_customers`
3. `last_n_events`
4. `reverse_first_k`

Queue with supporting state:

5. `moving_averages`
6. `first_non_repeating_stream`
7. `time_to_buy_tickets`

Repeated processing:

8. `round_robin_completion_order`
9. `generate_binary_numbers`

Medium deque pattern:

10. `sliding_window_maximum`

For now, implement only `process_queue_operations` and `serve_customers`.
Draw the queue after every enqueue, dequeue, or peek before writing code.
