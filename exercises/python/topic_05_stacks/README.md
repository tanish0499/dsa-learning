# Topic 05 - Stack Exercises

Read `lessons/05_stacks.md` first.

Implement one function at a time in `stacks.py`, then run:

```powershell
python -m unittest discover -s exercises/python
```

Unimplemented functions are skipped.

## Order

Stack operation warm-ups:

1. `reverse_with_stack`
2. `apply_backspaces`
3. `is_balanced_parentheses`
4. `remove_adjacent_duplicates`

Expression and path practice:

5. `evaluate_postfix`
6. `simplify_unix_path`
7. `validate_stack_sequences`

Monotonic-stack practice:

8. `next_greater_values`
9. `daily_warmer_waits`
10. `stock_spans`

For now, implement only `reverse_with_stack` and `apply_backspaces`. First draw
the stack after every push and pop.
