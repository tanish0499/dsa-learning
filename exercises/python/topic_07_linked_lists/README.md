# Topic 07 - Linked-List Exercises

Read `lessons/07_linked_lists.md` first.

Implement one function at a time in `linked_lists.py`, then run:

```powershell
python -m unittest discover -s exercises/python
```

Unimplemented functions are skipped.

## Order

Traversal warm-ups:

1. `linked_list_length`
2. `contains_value`
3. `value_at_index`

Changing links:

4. `append_value`
5. `delete_first_value`
6. `reverse_linked_list`

Pointer patterns:

7. `middle_value`
8. `merge_sorted_lists`
9. `remove_nth_from_end`
10. `has_cycle`

For now, implement only `linked_list_length` and `contains_value`. Draw
`current` above the node it references after every movement.
